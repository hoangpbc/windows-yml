---
- name: Check and Install Windows Updates (No Reboot)
  hosts: all  # Thay đổi theo inventory của bạn
  gather_facts: false
  tasks:
    - name: Check for available Windows updates
      win_updates:
        category_names:
          - SecurityUpdates
          - CriticalUpdates
          - UpdateRollups
          - FeaturePacks
          - ServicePacks
          - DefinitionUpdates
          - Drivers
          - Tools
          - Updates # Kiểm tra tất cả các bản cập nhật
        reboot: no
      register: update_result

    - name: Display available updates
      debug:
        msg: "Available updates: {{ update_result.updates | default([]) }}"

    - name: Install all available updates without rebooting
      win_updates:
        category_names:
          - SecurityUpdates
          - CriticalUpdates
          - UpdateRollups
          - FeaturePacks
          - ServicePacks
          - DefinitionUpdates
          - Drivers
          - Tools
          - Updates # Cài đặt tất cả các bản cập nhật
        reboot: no
      when: update_result.updates | length > 0

    - name: Show final update status
      debug:
        msg: "Installed updates: {{ update_result.updates | default([]) }}"

